# PointsBoard v11.3.7 Updated Files

Overlay this folder onto the root of the existing `flights` repository.

This patch preserves the v11.3.5 UI and applies only:

- Add a Route moved above Saved Routes.
- Reward search cash-fare results automatically populate the existing Cash Fares tab for the first selected route.
- One red-airplane favicon with no duplicate airplane in the page title.
- The old simplified static preview now redirects to the actual app.

See `UPDATE_INSTRUCTIONS_V11_3_7.md` before deployment.

# PointsBoard v11.3.3 Updated Files Patch

Overlay this patch onto an existing PointsBoard v11.3.2 `flights` repository. Do not use it as a new standalone installation because unchanged source, Worker, public, and configuration files are intentionally omitted.

After copying, commit and push to `main`, then verify GitHub Actions, Cloudflare deployment, and `/api/health` version 11.3.3.
